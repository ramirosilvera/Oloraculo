// Stub — uses loadEvaluations() + client-side aggregation by model name
export function PerformancePage() {
  return <div className="p-6 text-gray-400">Página de rendimiento — implementación completa en Fase 3</div>;
}
