// Stub — full implementation follows OracleLab.razor + Matches.razor pattern
// Uses: useAppData, engine.predict(), saveMatchSnapshot(), loadMatchSnapshots()
export function MatchesPage() {
  return <div className="p-6 text-gray-400">Página de partidos — implementación completa en Fase 3</div>;
}
