// Stub — triggers Cloudflare Workers via fetch() calls
// Buttons: "Actualizar rankings" → /ranking-refresh/rankings/fifa + elo
//          "Actualizar API-Football" → /api-football/fixtures?...
//          "Actualizar disponibilidad" → /availability-news/availability/refresh
export function DataPage() {
  return <div className="p-6 text-gray-400">Página de datos — implementación completa en Fase 4</div>;
}
